# Day1notes
## command line 
```
$ python -m SimpleHTTPServer 8080
$ mkdir todo-static
$ touch index.html
```
## git commands 
```
$ git status 
$ git staus 
$ git add -A
$ git diff --staged 
$ git commit -m "Initial commit"
$ git push origin master
```
